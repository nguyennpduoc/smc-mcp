## Complexity Assessment

Complexity: Medium
Rationale: Automatically assessed for compatibility.

## Goal
Ship a complete, buildable Model Context Protocol (stdio, TypeScript) server that gives any MCP client AI-powered smart-contract analysis backed by the Cysic AI model (MiniMax M3), with three named innovations (hybrid static+LLM pipeline, closed-loop self-audit, adversarial false-positive filter), full documentation, and unit tests, all in a single 3-role workflow (PLANNER → BUILDER → REVIEWER).

## Context
- Workspace: `/home/harness/cyops_data/workspace/smc-mcp-39833d`
- Stack: Node ≥20, TypeScript 5.6, `@modelcontextprotocol/sdk@1.0.4`, axios 1.7, zod 3.23, `@solidity-parser/parser@0.18`, dotenv 16.4, vitest 2.1. ESM (`"type": "module"`), `module/moduleResolution: Node16`.
- Cysic backend reference (exact shape required): `axios.post(${baseUrl}/chat/completions, { model, messages }, { headers: { Content-Type, Authorization: 'Bearer …' } })`, default `model: 'minimax-m3'`, return `response.data.choices[0].message.content`.
- Current repo state (BUILDER has completed most of the engineering):
  - `package.json`, `tsconfig.json`, `vitest.config.ts`, `.env.example`, `.gitignore`, `LICENSE` are in place; `node_modules/` and `dist/` are present; `npm run build` succeeds (dist files present).
  - `src/config.ts` — zod env schema, fail-fast on missing key, key fingerprint for logs.
  - `src/cysicClient.ts` — exact reference shape + timeout + retry/exponential backoff on 408/425/429/5xx + `CysicError` typed mapping (`auth|rate_limit|server|client|network|bad_response|config`) + key never logged.
  - `src/cache.ts` — SHA-256-keyed LRU `ResponseCache` with `stats()`, default cap 512.
  - `src/analysis/static.ts` — Solidity AST + line-based pattern checks (reentrancy, tx.origin, unchecked low-level calls, unchecked blocks, selfdestruct, floating pragma, block.timestamp, weak randomness, assembly, missing visibility) + `computeRiskScore` (severity-weighted 0..100).
  - `src/llm/reasoner.ts` — Pass-1 (verdicts on structured findings + additional findings), Pass-2 adversarial refuter on uncertain findings, `confidenceFloor=0.45`, cache-aware, returns `{confirmedFindings, rejectedFindings, additionalFindings, summary, llmCalls, cacheHits}`.
  - `src/llm/patcher.ts` — Given one `Finding`, asks the LLM for a patched full-file source (fences stripped).
  - `src/tools/audit_contract.ts` — Hybrid pipeline; accepts `{source}` or `{address, chain, rpcUrl, apiKey}`; persists to `AuditStore`; returns `resourceUri: contract://audits/{id}`.
  - `src/tools/suggest_fix.ts` — Patch + re-audit + before/after risk score + unified diff + `findingResolved` + `newFindingIds`.
  - `src/tools/explain_contract.ts` — Plain-English summary using static facts.
  - `src/tools/generate_contract.ts` — Spec → Solidity; optional pre-audit of generated code.
  - `src/tools/index.ts` — `ALL_TOOLS` registry, `getTool(name)`.
  - `src/server.ts` — MCP `Server` + `StdioServerTransport`; wires `ListTools`, `CallTool`, `ListResources`, `ListResourceTemplates`, `ReadResource`, `ListPrompts`, `GetPrompt`; safe error handler that redacts `CysicError` (no key).
  - `src/index.ts` — Loads env, calls `start()`, writes fatal errors to stderr.
  - `src/onchain/fetchSource.ts` — Etherscan-compatible explorer source fetch.
  - `src/audits/store.ts` — In-memory store with `save/get/list` for audit reports.
  - `src/resources/audits.ts` — `contract://audits` index and `contract://audits/{id}` reads.
  - `src/prompts/index.ts` — `security_audit`, `gas_review` prompts.
  - `src/util/diff.ts` — Unified diff helper for `suggest_fix`.
  - `tests/static.test.ts` — 13 unit tests covering rule catalogue, severity order, parse-error tolerance, risk scoring.
  - `tests/cysicClient.test.ts` — Mocks axios; covers exact POST shape, Bearer auth, model `minimax-m3`, retries on 429, classification of errors, no-key logging.
- What's missing for the spec to be fully satisfied:
  - `README.md` at repo root (problem/solution, architecture + data flow, FEATURE CHECKLIST, install + env, MCP-client registration JSON, example tool calls with sample output).
  - `docs/DESIGN.md` (3 innovations + rationale).
  - `docs/ARCHITECTURE.md` (component diagram, data flow, module responsibilities).
  - `docs/AGENTS.md` (3-role workflow, what each role produces and verifies).
  - Unit tests for `reasoner.ts` (verdict classification, confidence floor, refuter, cache hits), `patcher.ts` (fence stripping + cache), `tools/*` (audit_contract / suggest_fix / explain_contract / generate_contract happy paths), `audits/store.ts`, `resources/audits.ts`, `prompts/index.ts`, `util/diff.ts`, `fetchSource.ts` (malformed response, unsupported chain, http error).
  - `vitest.config.ts` exists but should explicitly include the `tests/` dir (verify in REVIEWER).

## Assumptions
- BUILDER keeps the existing code untouched unless the spec or plan explicitly requires a change. The shipped code already matches the Cysic reference shape and the 3-role rubric; the remaining work is documentation and test depth.
- The MCP-client registration JSON in the README follows the standard stdio-server shape (`command`, `args`, optional `env`) suitable for direct copy into Claude Desktop / Cursor / Continue / Cline configs.
- "Sample output" in the README is illustrative JSON that matches the tool result shapes; it does not need to come from a live LLM call.
- `vitest.config.ts` is configured to pick up `tests/**/*.test.ts`. If it doesn't, the BUILDER will fix it (no production code changes).
- The 3-role workflow is implemented as three sequential role agents (not concurrent); exactly three agent invocations: PLANNER (this file), BUILDER (implementation), REVIEWER (verification). No additional agents.
- The Cysic `baseUrl` and `model` defaults (`https://token-ai.cysic.xyz/v1`, `minimax-m3`) come from `.env.example` and `src/config.ts`; no live network call is required to satisfy the build/test rubric.

## Acceptance Criteria

- AC-1: `npm run build` exits 0 with no TypeScript errors and produces `dist/index.js` + `dist/server.js` (+ sourcemaps + `.d.ts`).
  - Key files: `tsconfig.json`, `src/**/*.ts`.
  - Verification: `npm run typecheck` then `npm run build`.

- AC-2: `npm test` runs the full vitest suite, exits 0, and includes tests for at least: the static analyzer (rule catalogue + severity order + risk scoring), the Cysic client (exact POST shape, Bearer auth, `minimax-m3` model, retries on 429/5xx, `CysicError` mapping, no API key in any error path or log), the reasoner (verdicts applied, confidence floor, refuter drops low-confidence), the patcher (fence stripping, cache reuse), the diff util, the audit store, the resource read handler, the prompts renderer, and the source fetcher (unknown chain, http error).
  - Key files: `tests/**/*.test.ts`, `vitest.config.ts`.
  - Verification: `npm test --silent`.

- AC-3: `src/cysicClient.ts` matches the reference Cysic call exactly: `axios.post(url, data, headers)` where `url = ${baseUrl}/chat/completions`, `data = { model, messages }`, `headers = { 'Content-Type': 'application/json', Authorization: 'Bearer ' + apiKey }`, default `model = 'minimax-m3'`, returns `response.data.choices[0].message.content`. On top, it adds: axios `timeout` from config, retry with exponential backoff + jitter on 408/425/429/5xx, `CysicError` typed mapping, and never logs the API key.
  - Key files: `src/cysicClient.ts`, `src/config.ts`, `tests/cysicClient.test.ts`.
  - Verification: `grep -n "Bearer" src/cysicClient.ts` shows the key only in the header construction; test asserts key absent from any error message; `grep -nE "console\.(log|info).*api[_-]?key" -ri src` returns no matches.

- AC-4: The MCP server exposes exactly the four tools (`audit_contract`, `explain_contract`, `suggest_fix`, `generate_contract`), exactly one resource template (`contract://audits/{id}`) plus the `contract://audits` index resource, and exactly two prompts (`security_audit`, `gas_review`). The tool registry, resource list, and prompt list are wired in `src/server.ts` and `src/tools/index.ts`.
  - Key files: `src/server.ts`, `src/tools/index.ts`, `src/resources/audits.ts`, `src/prompts/index.ts`.
  - Verification: `grep -nE "name: '" src/tools/index.ts src/resources/audits.ts src/prompts/index.ts` lists exactly the expected names.

- AC-5: `audit_contract` accepts either `{ source, filename? }` or `{ address, chain, rpcUrl?, apiKey? }`, returns a ranked vulnerability report, and persists an audit report retrievable at `contract://audits/{id}`. Re-running the same source returns the same `id` via cache-keyed model calls.
  - Key files: `src/tools/audit_contract.ts`, `src/audits/store.ts`, `src/cache.ts`.
  - Verification: `npm test --silent` exercises the handler; `node -e "…"` smoke call (manual) returns JSON with `audit.id`, `audit.confirmedFindings`, `audit.resourceUri`.

- AC-6: `suggest_fix` takes `{ source, finding_id, filename? }`, generates a patch, re-runs the hybrid pipeline on the patched source, and returns `{ diff, patchedSource, before, reAudit, findingResolved, newFindingIds, llmCalls, cacheHits }`.
  - Key files: `src/tools/suggest_fix.ts`, `src/llm/patcher.ts`, `src/util/diff.ts`, `src/llm/reasoner.ts`.
  - Verification: unit test on the handler with a static-only fallback asserts shape; manual check that `diff` is unified diff and `findingResolved === true` for a known-finding seed.

- AC-7: `explain_contract` returns a plain-English summary with `{ summary, contracts, functions, notableFindings, filename? }` and uses the static analyzer's structured facts as the LLM context.
  - Key files: `src/tools/explain_contract.ts`, `src/analysis/static.ts`.
  - Verification: unit test asserts handler builds correct structured payload and calls `cysic.complete` once.

- AC-8: `generate_contract` accepts `{ spec, solidityVersion?, license?, preAudit? }`, returns `{ source, preAudit?, spec, solidityVersion, license }`, and runs the static analyzer on the output when `preAudit !== false`.
  - Key files: `src/tools/generate_contract.ts`, `src/analysis/static.ts`.
  - Verification: unit test with mocked `cysic.complete` returns expected shape; integration test (manual) shows preAudit severity counts.

- AC-9: Innovation #1 (Hybrid pipeline) — the analyzer is deterministic and runs BEFORE the LLM. The LLM receives ONLY structured findings (id, ruleId, category, title, severity, line, evidence) — never the raw source alone — and reasons over those facts. `docs/DESIGN.md` explains the "facts over vibes" rationale and why it cuts hallucination vs naive prompting.
  - Key files: `src/analysis/static.ts`, `src/llm/reasoner.ts`, `docs/DESIGN.md`.
  - Verification: `grep -n "STATIC FINDINGS" src/llm/reasoner.ts` shows the prompt header; `docs/DESIGN.md` contains a §"Innovation #1" section with the rationale.

- AC-10: Innovation #2 (Closed-loop self-audit) — `suggest_fix` runs the analyzer + LLM on the patched source, returns a unified diff, and a before/after risk score with a `findingResolved` boolean. `docs/DESIGN.md` documents the flow.
  - Key files: `src/tools/suggest_fix.ts`, `src/util/diff.ts`, `docs/DESIGN.md`.
  - Verification: handler unit test exercises before/after; `docs/DESIGN.md` contains a §"Innovation #2" section.

- AC-11: Innovation #3 (Adversarial false-positive filter) — the reasoner applies a confidence floor (default 0.45) and runs a second "refuter" pass on uncertain findings, dropping those the refuter rejects. The static analyzer's findings are filtered through this gate before reaching the final report.
  - Key files: `src/llm/reasoner.ts`, `src/tools/audit_contract.ts`, `docs/DESIGN.md`.
  - Verification: `grep -n "REFUTER_PROMPT" src/llm/reasoner.ts`; `docs/DESIGN.md` contains a §"Innovation #3" section; reasoner unit test asserts low-confidence findings are rejected.

- AC-12: Content-hash cache is keyed on (model, system, prompt, temperature, source), survives within a process, and is exposed in the audit / suggest_fix response as `cacheHits` and `llmCalls`. Reproducibility test: running `reasonAboutContract` twice with the same input only makes one LLM call.
  - Key files: `src/cache.ts`, `src/llm/reasoner.ts`, `src/llm/patcher.ts`, `src/tools/audit_contract.ts`, `src/tools/suggest_fix.ts`.
  - Verification: unit test for `ResponseCache` (LRU, eviction, stats) and a reasoner test that asserts second call returns `cacheHits: 1` with one underlying call.

- AC-13: `README.md` exists at the repo root and contains, in order: (a) problem statement, (b) solution summary, (c) architecture + data-flow diagram (ASCII is fine), (d) FEATURE CHECKLIST with one row per shipped tool / resource / prompt / file, each row marked ✅ with a one-line description and the file path, (e) install + env instructions, (f) MCP-client registration JSON, (g) at least three example tool calls (one per primary tool) with sample output JSON.
  - Key file: `README.md` (to be created by BUILDER).
  - Verification: `head -1 README.md` exists; `grep -c "^- \[x\]" README.md` ≥ 8 (4 tools + 1 resource + 1 resource template + 2 prompts + the cache and store files).

- AC-14: `docs/DESIGN.md`, `docs/ARCHITECTURE.md`, and `docs/AGENTS.md` exist, are non-empty, and document the three innovations, the data flow / module layout, and the Planner→Builder→Reviewer 3-role workflow respectively.
  - Key files: `docs/DESIGN.md`, `docs/ARCHITECTURE.md`, `docs/AGENTS.md`.
  - Verification: `wc -l docs/DESIGN.md docs/ARCHITECTURE.md docs/AGENTS.md` each ≥ 40 lines; each doc has the required sections.

- AC-15: Env configuration is fail-fast. Starting the server without `CYSIC_API_KEY` exits 1 with a clean error and never logs the key. The `.env.example` ships with safe defaults; `.gitignore` excludes `.env`.
  - Key files: `src/config.ts`, `src/index.ts`, `src/server.ts`, `.env.example`, `.gitignore`.
  - Verification: `grep -n "CYSIC_API_KEY" .env.example`; `grep -n ".env" .gitignore`; `CYSIC_API_KEY= node dist/index.js` (or equivalent unit test) exits 1.

- AC-16: No hardcoded secrets anywhere in `src/`, `tests/`, or `dist/`. The only place the key string is constructed is the `Authorization: Bearer ${cfg.apiKey}` line in `src/cysicClient.ts`. The fingerprint function in `src/config.ts` is the only place a derivative of the key appears in logs.
  - Verification: `grep -rE "cysic[-_]?api[-_]?key|sk-[A-Za-z0-9]{8,}" src tests` returns no matches; `grep -rn "apiKey" src | grep -v "cysicClient.ts:18" src/config.ts tests` returns no other construction sites.

- AC-17: `package.json` declares scripts `build`, `start`, `dev`, `test` (and `test:watch`, `typecheck` are nice-to-haves), pins all runtime and dev dependencies, and requires `node >=20`.
  - Key file: `package.json`.
  - Verification: `node -e "JSON.parse(require('fs').readFileSync('package.json'))" && grep -E "build|start|dev|test" package.json`.

- AC-18: Unit tests for `cysicClient` mock axios (no real network) and cover at minimum: exact POST shape, Bearer auth header, default model `minimax-m3`, retry on 429 then success, retry exhaustion, mapping of 401→`auth`, 5xx→`server`, network error→`network`, malformed body→`bad_response`, and that no assertion ever contains the literal `TEST_KEY` in the error message.
  - Key file: `tests/cysicClient.test.ts`.
  - Verification: `npm test --silent`; `grep -n "TEST_KEY" tests/cysicClient.test.ts` shows the key only in fixture definitions.

- AC-19: The 3-role workflow constraint is honored. No additional agents are spawned. Exactly three role invocations occur in order: PLANNER (this plan + the README FEATURE CHECKLIST), BUILDER (implementation), REVIEWER (verification pass).
  - Verification: workflow log shows three role invocations and no others; `docs/AGENTS.md` documents this.

## Verification Matrix
| AC | Files or Areas | Verification Command or Check | Success Signal |
|---|---|---|---|
| AC-1 | `tsconfig.json`, `src/**/*.ts` | `npm run typecheck && npm run build` | exit 0; `dist/index.js` exists and `node -e "require('./dist/index.js')"` is loadable (entry exits on its own, not on import) |
| AC-2 | `tests/**/*.test.ts`, `vitest.config.ts` | `npm test --silent` | exit 0; all tests pass; coverage includes static, cysicClient, reasoner, patcher, diff, store, resource, prompt, fetchSource |
| AC-3 | `src/cysicClient.ts`, `src/config.ts`, `tests/cysicClient.test.ts` | `grep -n "Bearer" src/cysicClient.ts`; `npm test --silent` | header uses `Bearer ${cfg.apiKey}` only; tests assert no key in error messages; `console.*` calls never reference the key |
| AC-4 | `src/server.ts`, `src/tools/index.ts`, `src/resources/audits.ts`, `src/prompts/index.ts` | `grep -nE "name: 'audit_contract'|name: 'explain_contract'|name: 'suggest_fix'|name: 'generate_contract'" src/tools/index.ts`; same for resources/prompts | exactly the four tool names; `contract://audits` index + `contract://audits/{id}` template; `security_audit` + `gas_review` |
| AC-5 | `src/tools/audit_contract.ts`, `src/audits/store.ts`, `src/cache.ts` | unit test for `handleAuditContract` with mocked `cysic.complete` | response includes `audit.id`, `audit.confirmedFindings`, `audit.resourceUri: 'contract://audits/…'`; resource read returns the same id |
| AC-6 | `src/tools/suggest_fix.ts`, `src/llm/patcher.ts`, `src/util/diff.ts` | unit test with mock | response includes `patch.diff`, `patch.patchedSource`, `before.riskScore`, `reAudit.riskScore`, `findingResolved`, `newFindingIds`, `llmCalls`, `cacheHits` |
| AC-7 | `src/tools/explain_contract.ts` | unit test with mock | response includes `summary` (string from mock), `contracts`, `functions`, `notableFindings` |
| AC-8 | `src/tools/generate_contract.ts` | unit test with mock | `preAudit` is present when default; absent when `preAudit: false`; `source` is the mocked model output with fences stripped |
| AC-9 | `src/analysis/static.ts`, `src/llm/reasoner.ts`, `docs/DESIGN.md` | `grep -n "STATIC FINDINGS" src/llm/reasoner.ts`; `grep -n "Innovation #1" docs/DESIGN.md` | prompt header present; §Innovation #1 in DESIGN.md with rationale |
| AC-10 | `src/tools/suggest_fix.ts`, `src/util/diff.ts`, `docs/DESIGN.md` | `grep -n "Innovation #2" docs/DESIGN.md`; unit test | §Innovation #2 in DESIGN.md; handler test asserts before/after fields |
| AC-11 | `src/llm/reasoner.ts`, `src/tools/audit_contract.ts`, `docs/DESIGN.md` | `grep -n "REFUTER_PROMPT" src/llm/reasoner.ts`; `grep -n "Innovation #3" docs/DESIGN.md` | refuter prompt exists; §Innovation #3 in DESIGN.md; reasoner unit test drops low-confidence |
| AC-12 | `src/cache.ts`, `src/llm/reasoner.ts`, `src/llm/patcher.ts` | unit test: `reasonAboutContract` twice on same input | second call's `llmCalls` is 0 (or 1 refuter only) and `cacheHits` ≥ 1 |
| AC-13 | `README.md` | `wc -l README.md`; `grep -c "^- \[x\]" README.md`; visual inspection | ≥ 120 lines; ≥ 8 checklist rows; contains MCP-client JSON block; ≥ 3 example tool calls with sample output |
| AC-14 | `docs/DESIGN.md`, `docs/ARCHITECTURE.md`, `docs/AGENTS.md` | `wc -l docs/*.md` | each ≥ 40 lines; each has the required sections |
| AC-15 | `src/config.ts`, `src/index.ts`, `src/server.ts`, `.env.example`, `.gitignore` | `CYSIC_API_KEY= node dist/index.js` (or `npm test` with env stripped) | exits 1 with non-zero code, error message names `CYSIC_API_KEY` but contains no key value |
| AC-16 | `src/**`, `tests/**` | `grep -rE "cysic[-_]?api[-_]?key|sk-[A-Za-z0-9]{8,}" src tests` | no matches outside fixture strings and the Bearer header construction |
| AC-17 | `package.json` | `node -e "JSON.parse(require('fs').readFileSync('package.json'))"`; `grep -E '"(build|start|dev|test)"' package.json` | all four scripts present; pinned versions; `engines.node` ≥ 20 |
| AC-18 | `tests/cysicClient.test.ts` | `npm test --silent`; `grep -n "TEST_KEY" tests/cysicClient.test.ts` | key appears only in test fixtures; all tests pass with mocked axios |
| AC-19 | workflow | visual / log inspection | exactly three role invocations; no extra agents spawned |

## Implementation Notes

The BUILDER's implementation work is **divided into two layers**: code that is already complete (do not re-implement unless AC-3/AC-12/AC-16 fail) and code that is missing or thin. BUILDER must:

### Layer A — preserve and verify existing code
- Do NOT change `src/cysicClient.ts`, `src/config.ts`, `src/cache.ts`, `src/analysis/static.ts`, `src/llm/reasoner.ts`, `src/llm/patcher.ts`, `src/server.ts`, `src/index.ts`, the four tool files, `src/resources/audits.ts`, `src/prompts/index.ts`, `src/audits/store.ts`, `src/onchain/fetchSource.ts`, `src/util/diff.ts` unless a verification step (AC-2, AC-16) fails. If something is broken, fix it with the smallest possible change.
- `tsconfig.json` and `package.json` are pinned — keep all versions.

### Layer B — produce missing artifacts
1. **`README.md`** (new file, repo root).
   - Section order: Problem → Solution → Architecture + Data Flow (ASCII) → FEATURE CHECKLIST → Install → Env → MCP Client Registration → Example Tool Calls.
   - **FEATURE CHECKLIST content** (this is the PLANNER's deliverable handed to the BUILDER; the BUILDER copies it verbatim into `README.md`):

   ```markdown
   ## FEATURE CHECKLIST

   ### Tools
   - [x] `audit_contract` — Hybrid static+LLM audit with adversarial FP filter. → `src/tools/audit_contract.ts`
   - [x] `explain_contract` — Plain-English summary using static facts. → `src/tools/explain_contract.ts`
   - [x] `suggest_fix` — Patch + re-audit + before/after risk score. → `src/tools/suggest_fix.ts`
   - [x] `generate_contract` — Spec → Solidity + optional pre-audit. → `src/tools/generate_contract.ts`

   ### Resources
   - [x] `contract://audits` — Index of stored audit reports. → `src/resources/audits.ts`
   - [x] `contract://audits/{id}` — Read a single audit report. → `src/resources/audits.ts`

   ### Prompts
   - [x] `security_audit` — Drives `audit_contract` end-to-end. → `src/prompts/index.ts`
   - [x] `gas_review` — Gas-focused review driven by `explain_contract`. → `src/prompts/index.ts`

   ### Core modules
   - [x] Cysic client (axios + retry + key safety). → `src/cysicClient.ts`
   - [x] Env config + fail-fast + key fingerprint. → `src/config.ts`
   - [x] Static analyzer (AST + patterns + risk score). → `src/analysis/static.ts`
   - [x] Hybrid reasoner (verdicts + adversarial refuter). → `src/llm/reasoner.ts`
   - [x] LLM-driven patcher. → `src/llm/patcher.ts`
   - [x] SHA-256 keyed LRU response cache. → `src/cache.ts`
   - [x] Audit store (save / get / list). → `src/audits/store.ts`
   - [x] On-chain source fetcher (Etherscan-compatible). → `src/onchain/fetchSource.ts`
   - [x] MCP server wiring (stdio). → `src/server.ts`
   - [x] Stdio entry point. → `src/index.ts`

   ### Innovations
   - [x] Hybrid pipeline (deterministic static first; LLM reasons over structured facts). → `src/analysis/static.ts`, `src/llm/reasoner.ts`, `docs/DESIGN.md`
   - [x] Closed-loop self-audit (`suggest_fix` re-runs the pipeline on the patched source). → `src/tools/suggest_fix.ts`, `src/llm/patcher.ts`, `docs/DESIGN.md`
   - [x] Adversarial false-positive filter (refuter pass on uncertain findings). → `src/llm/reasoner.ts`, `docs/DESIGN.md`
   - [x] Content-hash cache for reproducibility. → `src/cache.ts`

   ### Documentation
   - [x] `README.md` — install, env, MCP-client JSON, examples.
   - [x] `docs/DESIGN.md` — three innovations + rationale.
   - [x] `docs/ARCHITECTURE.md` — components + data flow.
   - [x] `docs/AGENTS.md` — 3-role workflow.
   - [x] `.env.example` — env template.
   - [x] `LICENSE` — MIT.
   ```

   - **MCP-client registration JSON** (insert as a fenced ```json block):